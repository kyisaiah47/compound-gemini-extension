{
  "name": "compound-core",
  "version": "0.1.0",
  "description": "Financial-document extraction (invoices, receipts, statements, tables), PII redaction, and contract review from ParseRail — schema-validated JSON, billed only on success.",
  "mcpServers": {
    "compound-core": {
      "command": "npx",
      "args": ["-y", "@compound/api-mcp"]
    }
  },
  "contextFileName": "GEMINI.md",
  "settings": [
    {
      "name": "ParseRail API Key",
      "description": "Your ParseRail API key (ksk_live_…). Free at https://api.thecompound.tech — 500 credits/month, no card required.",
      "envVar": "COMPOUND_API_KEY",
      "sensitive": true
    }
  ]
}
